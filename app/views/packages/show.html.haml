%h1= @package.name

%p= link_to("Download Latest", download_package_path(@package))

%p
  - @package.versions.each do |version|
    = link_to(version.number, download_package_path(@package, :version => version.number))

%p== Downloads: #{@package.downloads}

%p= @package.summary
%p= @package.description
